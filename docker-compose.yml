version: '3.8'

services:
  backend:
    image: registry.digitalocean.com/avertai/backend:latest
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./model_registry:/app/model_registry
